version: "3"
services:
  frontend-cmp-admin:
    build: 
      context: ./services/frontend-cmp-admin
    image: frontend-cmp-admin:latest
    ports:
      - "3000:3000"
    networks:
      - concur-cfc-network
